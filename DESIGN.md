# Design — 工具箱 TOOLBOX

<!-- impeccable:design-schema 1 -->

方向：精密仪器台（direction #7，seed c5f6884b，Operate 模式）。本文档由实际实现回写，是已落地设计系统的单一权威描述。

## 世界

拒绝「圆角卡片 + 图标 + 文案」的通用工具箱模板。站点是一台精密仪器台：每个工具是一台仪表（panel），首页是仪表阵列（console），导航是阵列目录。访客把站点当作一台可靠的精密仪器——每台工具都有状态指示灯，任何操作都有即时的「亮灯」反馈。

## 色板

- 浅色（默认）：底 `#f5f3ef`（暖白纸感）、浮层 `#ffffff`、凹陷 `#faf9f6`；文字炭墨 `#23201a` / `#5c564b` / `#8e887b`；分隔线 `#e6e2d9` / `#d3cdbf`。
- **单一琥珀信号色 `#c2410c`**：只出现在可操作与成功态（主按钮、链接、状态灯、翻牌码高亮）；`#fbe7d3` 为选中/悬浮底色。
- 语义：成功 `#15803d`、错误 `#b91c1c`，各配柔和底。
- 深色（仪表台夜航）：底 `#161310`、浮层 `#1f1b16`、文字 `#f3efe8`；琥珀亮 `#fb923c`。跟随系统 + 手动切换（localStorage `toolbox-theme`）。

## 字体

- 正文：Inter（回退系统中文字体栈）。
- 编码读数 / 蚀刻标签 / 数字：JetBrains Mono，tabular-nums。标签一律 11px 大写字母间距蚀刻式。

## 签名交互

1. **翻牌码（split-flap）**：提取码逐位级联旋转揭示（`flap-in`，70ms 级联，transform-origin 底部，琥珀高亮）。
2. **状态灯（lamp）**：dim → lit，操作反馈以灯点亮表达；进行中灯脉冲（`lamp-pulse` 1.6s）。
3. 控件反馈：hover 亮底、active 微缩、focus-visible 琥珀描边；主按钮 ≥44px 触控目标。

## 布局

- 桌面：264px 侧边栏（品牌 + 分组菜单「工具 / 更多」+ 主题切换 + GitHub）+ 主列；工具页双面板（发送 / 接收）并排。
- ≤1023px：顶栏 + 抽屉菜单（scrim 遮罩，28ms ease-out 滑入）；面板单列。
- ≤560px：翻牌码缩放（44×52），模块单列。
- 仪表卡：头部（编号读数 T-01 + 状态灯 + 状态词）+ 主体（图标、名称、描述、标签、CTA）。

## 组件与状态

- 发送面板：传输模式分段（在线直传 / 离线 24h）× 内容分段（文本 / 文件）；拖拽上传 ≤10 个；生成后翻牌码 + 二维码 + 复制码/链接 + 进度条 + 再发/删除 + 24h 倒计时。
- 接收面板：6 位码输入（自动剥离非数字）；先探测 P2P 房间再退离线；文本/文件展示与下载。
- 反馈：alert（err/ok/info）+ spinner + progress-bar；状态永不单靠颜色（同时携带文字/图标/灯）。

## 动效

- fade-rise 入场（0.45s）；翻牌级联（0.5s）；灯脉冲（1.6s）；统一 `cubic-bezier(0.22,1,0.36,1)`。
- `prefers-reduced-motion` 全部收敛至近零时长。

## 反模式规避

- 无 emoji 图标（30+ 线性 SVG 图标集，`components/icons.tsx`）。
- 无紫色系渐变；琥珀为保留色不滥用。
- 状态非颜色单信号；错误信息贴近字段。
- 文本标签全中文通俗表达（“生成连接码 / 取回内容 / 再发一份”）。

## Provenance

- 挑战者参考板（impeccable concept-seed，seed c5f6884b）：方向 #1 舞台灯光、#2 深渊潜航、#3 暖色消费应用、#4 信号线路、#5 翻牌矩阵、#6 街机像素、#7 精密仪器台。
- Raises 已吸收：状态非颜色单信号（#1）、单一主轴（#2）、dim→lit 亮灯（#4）、琥珀保留（#6）、翻牌级联（#5）、触控 ≥44px 与通俗中文（#3）。
- 浏览器验证截图：/tmp/shot-home、shot-transfer、shot-send-ready、shot-recv-done、shot-home-dark、shot-mobile、shot-drawer（浅色/深色/移动端/抽屉/在线翻牌码/离线取回）。